<h1>マイレビュー一覧</h1>

 <table>
   <thead>
         <tr>
           <th></th>
           <th>レビュータイトル</th>
           <th>レビュー</th>
           <th>編集</th>
           <th>消去</th>
         </tr>
   </thead>
 <%# if review.user_id == current_user.id %>
    <tbody>
         <% @reviews.each do |review| %>
         <tr>
           <td><%= image_tag(review.image.attached? ? review.image : asset_url('no_image.jpg'), size: '100x100') %></td>
           <td><%= review.review_title %></td>
           <td><%= review.review_body %></td>
           <td><%= link_to "編集", edit_review_path(review.id) %></td>
           <td><%= link_to "消去", review_path(review.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
         </tr>
         <% end %><!--eachの格納されてるやつやからreview 最初@reciewにしてた-->
    </tbody>
     <%# end %>
<table>
  <%# else %>
  <p>レビューはありません</p>
<%# end %>